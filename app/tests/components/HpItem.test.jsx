var expect = require('expect');
var React = require('react');
var ReactDOM = require('react-dom');
var TestUtils = require('react-addons-test-utils');
var $ = require('jquery');

var HpItem = require('HpItem');

describe('HpItem', () => {
  it('should exist', () => {
    expect(HpItem).toExist();
  });
});
