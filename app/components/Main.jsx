var React = require('react');
var Header = require('Header');

var Main = React.createClass({
  render: function () {
    return (
      <div>
        <Header/>
      </div>
    );
  }
});

module.exports = Main;
